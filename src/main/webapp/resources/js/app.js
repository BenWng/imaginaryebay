'use strict';
(function(){
    var ShopApp=angular.module("ShopApp",["ngRoute", "ui.bootstrap",  "countdownTimer", "angularUtils.directives.dirDisqus","ngCookies"]);

    
     /*ShopApp.run(['$rootScope',function($rootScope){
     console.log($rootScope.test);
     }]);
    */
    
    
}());